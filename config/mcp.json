{
  "_NOTE": "Replace server entries below with your own MCP servers. The entries shown are examples only — do not commit tokens or credentials here. Use environment variables or a secrets manager instead.",
  "mcpServers": {
    "ai-assistant": {
      "command": "bash",
      "args": ["-c", "$HOME/.claude/scripts/ai-assistant-launch.sh"]
    },
    "asana": {
      "type": "sse",
      "url": "https://mcp.asana.com/sse"
    },
    "vercel": {
      "command": "bash",
      "args": ["-c", "$HOME/.claude/scripts/mcp-launch.sh npx -y vercel-mcp"],
      "env": {}
    }
  }
}
